# 🐳 Docker Quick Start Guide

## 🚀 Запуск системы в Docker

### 1. Запусти Docker Desktop
Открой Docker Desktop приложение на Mac (просто найди его в Applications или Spotlight)

### 2. Дождись запуска Docker
Когда в трее появится кит Docker - готово!

### 3. Одна команда для запуска всего:
```bash
cd /Users/a1/Desktop/codes/karma-app
make dev
```

### 4. Готово! 🎉
- **Frontend**: http://localhost:3000
- **Backend**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

## 🔧 Полезные команды:

```bash
make status        # Проверить что работает
make logs          # Посмотреть логи
make dev-down      # Остановить всё
make clean         # Почистить если что-то сломалось
```

## 💡 Преимущества:

✅ **Больше никаких падений** - каждый сервис в своем контейнере  
✅ **Порты не конфликтуют** - Docker сам управляет  
✅ **Одна команда для всего** - `make dev` и всё работает  
✅ **Изолированная база данных** - PostgreSQL в контейнере  
✅ **Hot reload** - изменения кода сразу видны  
✅ **Консистентная среда** - у всех одинаково работает  

## 🎯 Если что-то не работает:

1. Проверь что Docker запущен: `docker ps`
2. Почисти: `make clean`
3. Запусти заново: `make dev`

Всё! 